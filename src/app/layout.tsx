// Root layout - minimal wrapper for app
// The actual layout with fonts and providers is in [locale]/layout.tsx

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
